import styled from "styled-components";

export const CardStorage = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-evenly;
  flex-wrap: wrap;
  margin: 1em 2em;
`;